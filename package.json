{
  "//type": "module",
  "scripts": {
    "build": "rm -rf dist && npx parcel build --public-url ./ index.html",
    "dev": "parcel index.html",
    "gh-pages": "npm run build && gh-pages -d dist",
    "serve": "npm run build && serve dist"
  },
  "devDependencies": {
    "@parcel/transformer-elm": "^2.4.0",
    "parcel": "^2.4.0",
    "@types/node": "^17.0.23",
    "@types/ws": "^8.5.3",
    "elm": "^0.19.1-5",
    "gh-pages": "^3.2.3",
    "serve": "^13.0.2"
  }
}
